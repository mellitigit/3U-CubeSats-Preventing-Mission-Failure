# 🛰️ مشروع CubeSat Detumbling باستخدام التعلم بالتعزيز

## 📋 نظرة عامة

هذا المشروع يجمع بين **التعلم الموجه** (Supervised Learning) و**التعلم بالتعزيز** (Reinforcement Learning) لبناء نظام ذكي للتحكم في دوران القمر الصناعي الصغير CubeSat.

### المكونات الرئيسية:
1. **نموذج الكشف (Detector)**: شبكة عصبية مدربة على كشف حالة الاضطراب
2. **بيئة المحاكاة (CubeSatEnv)**: بيئة Gym لمحاكاة ديناميكية القمر الصناعي
3. **وكيل RL (SAC Agent)**: خوارزمية Soft Actor-Critic لتعلم سياسة التحكم الأمثل

---

## 🚀 البدء السريع

### 1. تثبيت المتطلبات

```bash
pip install -r requirements.txt
```

أو بشكل مباشر:
```bash
pip install numpy tensorflow scikit-learn matplotlib gym==0.26.2 stable-baselines3
```

### 2. تشغيل الدفاتر بالترتيب

#### أ) تدريب نموذج الكشف
افتح وشغّل `cubesat.ipynb`:
- يولّد بيانات اصطناعية
- يدرّب نموذج كشف الاضطراب
- يحفظ النموذج في `model_supervised.keras`

#### ب) تدريب وكيل التعلم بالتعزيز
افتح وشغّل `rl-cubesat.ipynb`:
- يحمّل نموذج الكشف المدرب
- يبني بيئة Gym للقمر الصناعي
- يدرّب وكيل SAC
- يقيّم الأداء ويحفظ السياسة

---

## 📁 هيكل الملفات

```
cubesat-rl/
├── cubesat.ipynb                      # الدفتر الأول: تدريب نموذج الكشف
├── rl-cubesat.ipynb                   # الدفتر الثاني: التعلم بالتعزيز
├── requirements.txt                   # المكتبات المطلوبة
├── README.md                          # هذا الملف
│
├── model_supervised.keras             # النموذج المدرب (يُنشأ بعد تشغيل الدفتر الأول)
├── best_detector_model.keras          # أفضل نموذج أثناء التدريب
│
├── sac_cubesat_policy.zip             # السياسة المدربة (يُنشأ بعد تشغيل الدفتر الثاني)
│
├── checkpoints/                       # نقاط التفتيش أثناء التدريب
├── logs/                              # سجلات TensorBoard
│
├── training_curves.png                # رسوم بيانية للتدريب
└── evaluation_results.png             # نتائج التقييم
```

---

## 🎯 الأهداف والنتائج

### الهدف الرئيسي
تعلم سياسة تحكم ذكية لإيقاف دوران القمر الصناعي (Detumbling) بأقل استهلاك للطاقة.

### مقاييس الأداء
- **السرعة الزاوية النهائية**: يجب أن تكون < 0.05 rad/s
- **عدد الخطوات**: كلما قل، كان أفضل (استقرار أسرع)
- **استهلاك الطاقة**: تقليل مجموع مربعات عزوم التحكم
- **احتمالية الاضطراب**: يجب أن تكون < 0.1 حسب نموذج الكشف

---

## 🔧 التحسينات المطبقة

### في `cubesat.ipynb`:
✅ تثبيت البذور العشوائية لإعادة إنتاج النتائج  
✅ إضافة EarlyStopping و ModelCheckpoint  
✅ حفظ النموذج بشكل صريح  
✅ مقاييس تفصيلية (Precision, Recall, Confusion Matrix)  
✅ رسم منحنيات الدقة والخسارة  
✅ اختبار تحميل النموذج المحفوظ  

### في `rl-cubesat.ipynb`:
✅ معالجة أخطاء تحميل النموذج (مسار مرن)  
✅ حذف الاستيرادات غير المستخدمة (torch)  
✅ إضافة حد أقصى للخطوات (max_steps=200)  
✅ تثبيت البذور العشوائية  
✅ زيادة عدد خطوات التدريب (من 5,000 إلى 50,000)  
✅ إضافة CheckpointCallback و TensorBoard  
✅ خلية تقييم شاملة مع رسوم بيانية  
✅ قص الأفعال (action clipping)  
✅ محاكاة تغيير المجال المغناطيسي  

---

## 📊 مراقبة التدريب

### استخدام TensorBoard

أثناء تدريب وكيل SAC، يمكنك مراقبة التقدم باستخدام:

```bash
tensorboard --logdir=./logs/sac_cubesat_tensorboard/
```

ثم افتح المتصفح على: `http://localhost:6006`

### المقاييس المتاحة:
- `rollout/ep_len_mean`: متوسط طول الحلقات
- `rollout/ep_rew_mean`: متوسط المكافأة
- `train/learning_rate`: معدل التعلم
- `train/loss`: خسارة التدريب

---

## 🧪 اختبار السياسة المدربة

بعد التدريب، يمكنك اختبار السياسة:

```python
from stable_baselines3 import SAC
import numpy as np

# تحميل السياسة
model = SAC.load("sac_cubesat_policy")

# إنشاء البيئة
env = CubeSatEnv()

# اختبار
obs = env.reset()
for _ in range(200):
    action, _ = model.predict(obs, deterministic=True)
    obs, reward, done, info = env.step(action)
    
    if done:
        print(f"تم الاستقرار في {info['step']} خطوة!")
        print(f"السرعة الزاوية النهائية: {info['angular_speed']:.4f} rad/s")
        break
```

---

## 🔬 التطويرات المستقبلية المقترحة

### 1. تحسين النموذج الفيزيائي
- [ ] إضافة معادلات أويلر الكاملة للدوران
- [ ] استخدام مصفوفة عطالة واقعية للقمر الصناعي
- [ ] محاكاة تأثير المجال المغناطيسي على العزم
- [ ] إضافة اضطرابات خارجية (solar pressure, gravity gradient)

### 2. تحسين البيانات
- [ ] استخدام بيانات محاكاة من GMAT أو STK
- [ ] إضافة ضوضاء واقعية للحساسات
- [ ] استخدام سلاسل زمنية بدلاً من عينات منفردة
- [ ] جمع بيانات من مهمات CubeSat حقيقية

### 3. تحسين التعلم
- [ ] تجربة خوارزميات أخرى (PPO, TD3, DDPG)
- [ ] استخدام بيئات متوازية لتسريع التدريب
- [ ] Hyperparameter tuning باستخدام Optuna
- [ ] Transfer Learning من مهمات مشابهة

### 4. التقييم والاختبار
- [ ] إنشاء مجموعة اختبار متنوعة (سيناريوهات مختلفة)
- [ ] مقارنة مع طرق التحكم التقليدية (B-dot, PD controller)
- [ ] اختبار الصلابة (Robustness) ضد اضطرابات غير متوقعة
- [ ] تحليل استهلاك الطاقة التراكمي

---

## 📚 مراجع ومصادر

### خوارزميات التعلم بالتعزيز:
- [Stable-Baselines3 Documentation](https://stable-baselines3.readthedocs.io/)
- [SAC Paper: Soft Actor-Critic](https://arxiv.org/abs/1801.01290)

### ديناميكية الأقمار الصناعية:
- Wertz, J. R. (2012). *Spacecraft Attitude Determination and Control*
- Sidi, M. J. (1997). *Spacecraft Dynamics and Control*

### CubeSat Detumbling:
- B-dot Control Algorithm
- Magnetorquer-based attitude control

---

## 🤝 المساهمة

لتحسين المشروع:
1. Fork المشروع
2. أنشئ فرعاً للميزة الجديدة
3. قم بعمل Commit للتغييرات
4. ادفع إلى الفرع
5. افتح Pull Request

---

## 📝 ملاحظات مهمة

⚠️ **هذا المشروع تعليمي**: النماذج الفيزيائية المستخدمة مبسطة جداً ولا تمثل الواقع بدقة كاملة.

⚠️ **البيانات الاصطناعية**: البيانات المستخدمة في التدريب مولدة بشكل عشوائي وليست من حساسات حقيقية.

⚠️ **الأداء**: للحصول على نتائج أفضل، زد عدد خطوات التدريب إلى 100,000+ واستخدم بيانات أكثر واقعية.

---

## 📧 الدعم

إذا واجهت مشاكل:
1. تأكد من تثبيت جميع المكتبات بالإصدارات الصحيحة
2. تحقق من وجود ملف `model_supervised.keras` قبل تشغيل دفتر RL
3. راجع رسائل الأخطاء في الخلايا

---

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام التعليمي والبحثي.

---

**تم الإنشاء بتاريخ**: نوفمبر 2025  
**آخر تحديث**: نوفمبر 2025

---

✨ **نتمنى لك تعلماً ممتعاً!** 🚀
